files:
  /etc/nginx/conf.d/other.conf:
    content: |
      fastcgi_buffers 16 16k;
      fastcgi_buffer_size 32k;

container_commands:
  00_nginx_worker_processes:
    command: aws s3 cp /etc/nginx/conf.d/*.conf s3://ip-docker
    ignoreErrors: false